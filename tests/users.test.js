const userFuncs = require('../data')
let user = userFuncs.userFuncs
let checkUser = user.checkUser

/** 
test.skip('Workaround', () => 1)
test('checks valid username & password', async () =>{
        const data = await checkUser('test@gmail.com', 'password')
        expect(data).toBe('user authenticated')
    });

*/